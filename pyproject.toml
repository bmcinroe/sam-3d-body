[project]
name = "sam-3d-body"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [] # Leave empty for submodule-ing

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sam_3d_body"]   